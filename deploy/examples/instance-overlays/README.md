# Instance overlays (not generic template defaults)

Copy these onto a **specific VPS** after clone. Do not treat as required template files.

| File | Use |
|------|-----|
| `REI-GHL-AGENT-PLAYBOOK.md` | REI vertical learnings — copy to `obsidian/GHL/` on REI instances |
| `../config/ghl-accounts.instance.rei.example.json` | Multi-account MCO/AVE/WNN registry |
| `../hermes-cockpit/` | Hermes UI theme + CEO cockpit plugin — `bash scripts/install-hermes-cockpit.sh` |

Generic template default: one `ghl` agent + `deploy/config/ghl-accounts.json`.
