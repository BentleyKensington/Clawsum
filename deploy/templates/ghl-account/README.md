# Deprecated — use `deploy/templates/ghl/`

REI-branded templates were removed from the generic repo. Use `templates/ghl/` for all new provisions.

Multi-account REI instance overlay: `deploy/config/ghl-accounts.instance.rei.example.json`
